define(['quark', 'knockout'], function($$, ko) {
    function MainController($imports) {
        $$.wait($imports.ready, function() {
            debugger;
        });
    }

    return MainController;
});
